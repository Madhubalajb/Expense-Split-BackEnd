{
  "name": "expense-split-backend",
  "version": "1.0.0",
  "description": "Backend for Expense Split App",
  "main": "server.js",
  "scripts": {
    "devStart": "nodemon server.js",
    "build:ui": "rm -rf build && cd ../expense-split && npm run build --prod && cp -r build ../expense-split-backend",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m ui_build && git push &7 npm run deploy"
  },
  "author": "Madhubala Jayakumaran",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.6.5"
  },
  "devDependencies": {
    "dotenv": "^8.0.0",
    "nodemon": "^1.19.1"
  }
}
